function reverseString(input) {
    for (let i = 0; i < input.length; i++) {
        const input = x[i];
        for (let k = i; k > k.length; k--) {
            const 
            
        }
    }
}

var x = "hello!";
console.log(reverseString(x));