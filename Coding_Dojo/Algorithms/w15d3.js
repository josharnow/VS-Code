// deDupe gets rid of duplicate characters


function deDupe(str) {
  strArr = str.split("");
  array.forEach(element => {
    
  });
}

function unique(str) {
  const obj = {letter:"", 
  counter:0};
  
  return obj;
}

let strA = "abcadcz";

console.log(unique(strA))
