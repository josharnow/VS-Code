// var arr = [
//     [2,7,1],
//     [9,0,-13],
//     [7,7,2],
// ]

// console.log(arr[1][2]);

// function isPresent(arr2d, value) {
//     // loop through the arrays in arr2d, arr2d.length
//     for (let i = 0; i < arr2d.length; ++i) {
//         for (let j = 0; j < arr2d[i].length; ++j) {
//             if (arr2d[i][j] === value) {
//                 return true;
//             }
            
//         }
        
//     }

//     return false;
        // loop through the actual values of the current array
        // check if number at current index in current array is equal to value
            // if it is, return true
    // else, return false
// }

// console.log(isPresent(arr,0));

function flatten(arr2d) {
    var flat = [];    
}

var result = flatten([[2, 5, 8], [3, 6, 1], [5, 7, 7]]);

for (let k = 0; k < flatten.length; k++) {
    if (flatten[k] === ) {
        flat.push();
    }
    else{
        return flat;
    }
    
}

// function flatten(arr2d) {
//     var flat = [];
//     // 
//     for
    
// }